# Half-spherical-twist
## How to compile the source files
Clone this repository and run
``` bash
latexmk src/*.tex
```
in the `half-spherical-twist` directory or run
``` bash
cd latexmk -r ../.latexmkrc *.tex
```
in the `half-spherical-twist/src` directory.